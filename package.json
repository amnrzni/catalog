{
  "name": "catalog",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "ui-ux-catalog"
  ],
  "scripts": {
    "build": "npm run build --workspace=ui-ux-catalog",
    "dev": "npm run dev --workspace=ui-ux-catalog",
    "start": "npm run start --workspace=ui-ux-catalog",
    "lint": "npm run lint --workspace=ui-ux-catalog"
  }
}
